import { BaseFormDirective } from './base-form.component';

describe('BaseFormDirective', () => {
  it('should create an instance', () => {
    const directive = new BaseFormDirective();
    expect(directive).toBeTruthy();
  });
});
