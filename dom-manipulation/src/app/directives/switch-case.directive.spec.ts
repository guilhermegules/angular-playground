import { SwitchCaseDirective } from './switch-case.directive';

describe('SwitchCaseDirective', () => {
  it('should create an instance', () => {
    const directive = new SwitchCaseDirective();
    expect(directive).toBeTruthy();
  });
});
